.DS_Store
target/
node_modules/
dist/
crates/witnos-app/gen/
# Staging dir for the CLI bin bundled into the .app (see install-app.sh).
crates/witnos-app/binaries/
# Dogfooding artifacts: witnos runtime state + the hooks it installs here
# (absolute machine-specific paths — never commit).
.witnos/
.claude/
# Local working documents (2026-08-03). They stay on disk and CLAUDE.md still
# points at them, because that file is read HERE where they exist; they are
# just not part of the published tree from this commit on. Earlier commits
# still contain them — deliberately, the history was not rewritten.
docs/
spike/
