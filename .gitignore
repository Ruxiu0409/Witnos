.DS_Store
target/
node_modules/
dist/
crates/witnos-app/gen/
# Staging dir for the CLI bin bundled into the .app (see install-app.sh).
crates/witnos-app/binaries/
# Dogfooding artifacts: witnos runtime state + the hooks it installs here
# (absolute machine-specific paths — never commit).
.witnos/
.claude/
# Local working documents stay private by default. The public design notes are
# explicit exceptions so the product READMEs can stay short without throwing
# away the reasoning behind the project.
docs/*
!docs/README.md
!docs/design.md
!docs/design.zh-Hant.md
!docs/design.ja.md
spike/
